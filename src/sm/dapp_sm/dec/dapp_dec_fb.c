#include "dapp_dec_fb.h"

#include <assert.h>

e2sm_dapp_event_trigger_t dapp_dec_event_trigger_fb(size_t len, uint8_t const ev_tr[len])
{
  assert(0 != 0 && "Not implemented");
  assert(ev_tr != NULL);
  e2sm_dapp_event_trigger_t avoid_warning;
  return avoid_warning;
}

dapp_action_def_t dapp_dec_action_def_fb(size_t len, uint8_t const action_def[len])
{
  assert(0 != 0 && "Not implemented");
  assert(action_def != NULL);
  dapp_action_def_t avoid_warning;
  return avoid_warning;
}

e2sm_dapp_ind_hdr_t dapp_dec_ind_hdr_fb(size_t len, uint8_t const ind_hdr[len])
{
  assert(0 != 0 && "Not implemented");
  assert(ind_hdr != NULL);
  e2sm_dapp_ind_hdr_t avoid_warning;
  return avoid_warning;
}

e2sm_dapp_ind_msg_t dapp_dec_ind_msg_fb(size_t len, uint8_t const ind_msg[len])
{
  assert(0 != 0 && "Not implemented");
  assert(ind_msg != NULL);
  e2sm_dapp_ind_msg_t avoid_warning;
  return avoid_warning;
}

dapp_ctrl_hdr_t dapp_dec_ctrl_hdr_fb(size_t len, uint8_t const ctrl_hdr[len])
{
  assert(0 != 0 && "Not implemented");
  assert(ctrl_hdr != NULL);
  dapp_ctrl_hdr_t avoid_warning;
  return avoid_warning;
}

dapp_ctrl_msg_t dapp_dec_ctrl_msg_fb(size_t len, uint8_t const ctrl_msg[len])
{
  assert(0 != 0 && "Not implemented");
  assert(ctrl_msg != NULL);
  dapp_ctrl_msg_t avoid_warning;
  return avoid_warning;
}

dapp_ctrl_out_t dapp_dec_ctrl_out_fb(size_t len, uint8_t const ctrl_out[len])
{
  assert(0 != 0 && "Not implemented");
  assert(ctrl_out != NULL);
  dapp_ctrl_out_t avoid_warning;
  return avoid_warning;
}

dapp_func_def_t dapp_dec_func_def_fb(size_t len, uint8_t const func_def[len])
{
  assert(0 != 0 && "Not implemented");
  assert(func_def != NULL);
  dapp_func_def_t avoid_warning;
  return avoid_warning;
}
