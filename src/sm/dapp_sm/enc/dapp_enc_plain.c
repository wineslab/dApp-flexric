#include "dapp_enc_plain.h"

#include <assert.h>
#include <stdlib.h>
#include <stdio.h>
#include <arpa/inet.h>

#include "byte_array.h"

byte_array_t dapp_enc_event_trigger_plain(e2sm_dapp_event_trigger_t const* event_trigger)
{
  assert(0 != 0 && "Not implemented");
  assert(event_trigger != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_action_def_plain(e2sm_dapp_action_def_t const* action_def)
{
  assert(0 != 0 && "Not implemented");
  assert(action_def != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_ind_hdr_plain(e2sm_dapp_ind_hdr_t const* ind_hdr)
{
  assert(0 != 0 && "Not implemented");
  assert(ind_hdr != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_ind_msg_plain(e2sm_dapp_ind_msg_t const* ind_msg)
{
  assert(0 != 0 && "Not implemented");
  assert(ind_msg != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_ctrl_hdr_plain(e2sm_dapp_ctrl_hdr_t const* ctrl_hdr)
{
  assert(0 != 0 && "Not implemented");
  assert(ctrl_hdr != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_ctrl_msg_plain(e2sm_dapp_ctrl_msg_t const* ctrl_msg)
{
  assert(0 != 0 && "Not implemented");
  assert(ctrl_msg != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_ctrl_out_plain(e2sm_dapp_ctrl_out_t const* ctrl)
{
  assert(0 != 0 && "Not implemented");
  assert(ctrl != NULL);
  byte_array_t ba = {0};
  return ba;
}

byte_array_t dapp_enc_func_def_plain(e2sm_dapp_func_def_t const* func)
{
  assert(0 != 0 && "Not implemented");
  assert(func != NULL);
  byte_array_t ba = {0};
  return ba;
}
